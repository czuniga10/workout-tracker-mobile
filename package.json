{
  "name": "workout-tracker",
  "private": true,
  "scripts": {
    "setup": "npm install && npm --prefix api install && npm --prefix web install && npm run db && npm --prefix api run db:deploy && npm --prefix api run prisma:generate",
    "predev": "npm run db && npm --prefix api run db:deploy",
    "dev": "concurrently --names api,web --prefix-colors cyan,magenta \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "db": "docker compose up db -d"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
